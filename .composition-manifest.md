---
kind: composition-manifest
feature: review-widget
feature_version: 1.0.0
composed_into: public/website/
status: stable
first_authored: pre-2026-05-12 (originated before atomic-composition discipline; no source manifest)
last_re_emitted: 2026-05-15 (commentLifecycleMode + chromeAnchored + MutationObserver + ANCHOR_TAGS_EXTRA — 5-PR window; supersedes 2026-05-13)
library_sha: 192de33 (upstream tnosugar/organization-template; f40cbe7..192de33 window)
sync_manifest_synced: 2026-05-13 (library/ subtrees mirrored from upstream main)
---

# review-widget composition manifest — public/website/

**2026-07-18 — pinned to `review-widget` `feature_version: 1.0.0`.** This deliverable now pins the first versioned release (`library/features/review-widget/CHANGELOG.md`); spec `last_updated` drift no longer implies re-emit. Re-emit only on a deliberate `feature_version` bump per its CHANGELOG class (patch=no / minor=opt-in / major=required). `kind:` also migrated `composition_manifest` → `composition-manifest` (hyphen canonical) on this touch. No behavioral change.

This sidecar records the L9 css-isolation re-emission of `public/website/review-mode.css` and `public/website/review-mode.js`. The original review widget composition predates the atomic-composition discipline, so there is no first-pass entry; the L9 re-emission is the first manifest entry. A future re-composition (or a per-project Cowork session) should backfill the original composition basis (library + variation references, brand-token resolution) so the deliverables become fully reproducible from spec.

**Do not hand-edit** the composed files (`review-mode.css`, `review-mode.js`, `review-bootstrap.js`, `contact-form.config.js`) without recording the change in `.claude/variations/review-widget.md`. A re-composition without that record will silently clobber your edits.

## 2026-05-15 re-emission — five-PR window (commentLifecycleMode + chromeAnchored + extensibility + entry-button recolor)

Re-composed `review-mode.{css,js}`, `review-bootstrap.js`, and `contact-form.config.js` against the upstream library window `f40cbe7..192de33` (organization-template `library/features/review-widget/`). Five PRs landed in that window:

1. **comment-lifecycle.md** — `commentLifecycleMode` config option (`'full'` default vs `'feedback-only'`). 3-state status enum (`pending` / `applied` / `archived`) with reader-side shim for legacy boolean records (`archived: true` / `applied: true`). `groupStatus()` vocabulary bridge (status enum per-comment, filter-tab key per-group).
2. **pending-archived-workflow.md** — 3 filter tabs (`active` / `applied` / `archived`); group-footer bulk-archive button suppressed in feedback-only mode.
3. **anchor-extensibility.md** — `ANCHOR_TAGS_EXTRA` config + `[data-comment-target]` per-element opt-in + `MutationObserver` for dynamic content. Sticky per-tag counter across init + observer-driven passes.
4. **commentable-everything.md** — `commentableContent: 'allowlist' | 'direct-text'` mode + `chromeAnchored: boolean` config. Chrome anchors use cross-page `chrome-{tag}-{n}` slugs and write `page: '__chrome__'`; subscription includes the `__chrome__` bucket so chrome comments surface on every page. `NEVER_ANCHOR` deny-list.
5. **inert-entry-button.md** — codifies the floating entry button as canonical; brand-token resolution (anti-pattern: hardcoded hex). Class is `.review-toggle-btn`.

### Prota's composition choices

| Config | Value | Rationale |
| --- | --- | --- |
| `commentLifecycleMode` | `'feedback-only'` | Vernon (reviewer) → Claude (operator who patches `status` outside the widget). In-UI Apply/Archive buttons would mislead — they'd skip Claude. Per `comment-lifecycle.md` §"`commentLifecycleMode: 'feedback-only'`", per-row buttons reduce to Edit/Delete (pending) and Restore/Delete (applied/archived); group-footer bulk-archive button suppressed. |
| `chromeAnchored` | `true` | Vernon comments on site chrome (nav links, footer, logo); chrome is structurally identical across pages. Forward-only adoption: zero existing comments use chrome anchors (verified via Firebase REST `shallow=false`), so no data migration needed. |
| `commentableContent` | unset (default `'allowlist'`) | Prota stays on the curated tag-list model rather than direct-text mode. |
| `ANCHOR_TAGS_EXTRA` | `[a, button, label, blockquote, figcaption, summary, details, b, i, u, mark, cite, q, code, pre, kbd, samp, var, time, abbr]` | Deviates from the recommendation of `[]` because Vernon's existing 29 comments include 3 on `<a>` and 1 on `<summary>` (both outside canonical `ANCHOR_TAGS`). Without these extras those comments would orphan to `(no anchor)`, and Vernon would lose the ability to comment on CTA links / case-study blockquotes / figure captions going forward. Per `anchor-strategy.md` anti-pattern, `div` is intentionally NOT in this list — `<div>` opt-in goes through `[data-comment-target]` instead. |

### Concrete emission changes

**`contact-form.config.js`** — three new opt-in keys appended to `PROTA_CONTACT_CONFIG` with inline rationale block above the object.

**`review-bootstrap.js`** — entry button recolored from odvaz forest `#2c5e4a` / cream `#fbf8f3` (cosmetic regression from initial copy-paste) to prota navy `#1F3864` / white `#ffffff`; hover background to `#142646`. Dropped `gap: 8px` and the `::before { content: "💬"; }` rule. Function renamed `injectKomentariButton` → `injectEntryButton`. Per `inert-entry-button.md` anti-pattern §"Hardcoding colors instead of resolving brand tokens", the resolved tokens are now used directly (and the prior bug is documented in the file's header comment for future re-emitter context).

**`review-mode.js`** — substantial rewrite (827 → 1081 lines):

- `getStatus(c)` reader-side shim normalizes legacy boolean records (`archived: true`, `applied: true`, even older `status: "dismissed"`) to the 3-state enum. `groupStatus(comments)` returns filter-tab keys (`active` / `applied` / `archived`), NOT status-enum values — the vocabulary bridge per `comment-lifecycle.md` §"Vocabulary bridge". Default `state.filter = 'active'`.
- Three filter tabs in the sidebar drawer; renderCommentList groups comments by anchor and filters by `groupStatus(group) === state.filter`.
- Module-scoped sticky counters `ANCHOR_COUNTERS` (page-scope) + `CHROME_COUNTERS` (cross-page). Never reset between init and MutationObserver-driven passes.
- Canonical `ANCHOR_TAGS` list (tight) + `ANCHOR_TAGS_EFFECTIVE = canonical ∪ ANCHOR_TAGS_EXTRA`. `NEVER_ANCHOR` deny-list emitted as defense-in-depth in both modes.
- `tryAnchor(el)`: widget-chrome skip → already-anchored skip → opt-out skip → mode-gated allowlist-vs-direct-text → chrome-route gate → ID assignment via `CHROME_COUNTERS` (chrome) or `ANCHOR_COUNTERS` (content). Image-wraps get tag bucket `'img'`.
- `setupDynamicAnchoring()`: `MutationObserver` scoped to content area, `{ childList: true, subtree: true }`. Each added element runs through `wrapImages` → `anchorSubtree` → `wireAnchorsIn`. Observer reference stored at `state.anchorObserver` for future disconnect.
- Two-pass `anchorSubtree`: tag-list pass first (deduped by `data-comment-id`), then `[data-comment-target]` attribute pass.
- Modal write: when anchor ID starts with `chrome-`, writes `page: '__chrome__'`; otherwise writes `state.pageSlug`. Subscription read accepts records where `c.page === state.pageSlug || c.page === '__chrome__'`.
- New `submitComment` writes `status: 'pending'` enum (instead of legacy `archived: false`). `applyComment` / `archiveComment` / `archiveComments` / `restoreComment` write to the enum field directly.
- `renderActions(id, status)` is the single source of truth for which buttons render per `(status, lifecycleMode)`; the 5 per-action button classes (`apply-btn` / `edit-btn` / `archive-btn` / `restore-btn` / `delete-btn`) all wired so flipping `commentLifecycleMode` to `'full'` later renders correctly with zero JS re-emit.
- Replaces the previous custom `computeStableId` / `computeNavId` / `computeFooterId` chrome-anchoring logic (which used href-slugs like `nav-cta-contact-protastudios-ai`) with the canonical sequential `chrome-{tag}-{n}` pattern. The c3 adoption path: zero existing comments use the legacy chrome slug shape (Firebase REST verified), so no anchor migration is needed; the new pattern starts fresh.

**`review-mode.css`** — minor (+15 lines):
- Adds `apply-btn:hover` (sage tint per `css-isolation.md` §"Per-action suffix classes") and `archive-btn:hover` (muted neutral tint). The 3 existing per-action hovers (`edit-btn`, `delete-btn`, `restore-btn`) get inline-comment annotations.
- No other rule changes. The CSS is mode-agnostic: emits styles for all 5 button classes even though `'feedback-only'` only renders Edit/Delete/Restore. A future config flip to `'full'` would render Apply/Archive without re-emitting the CSS.

### Migration scope

- **Legacy boolean records (26 of 29 existing comments).** Read transparently via `getStatus()` shim — no action required for the widget to function. A one-shot REST PATCH walking `/comments/` and rewriting `archived: true` → `status: 'archived'`, missing → `status: 'pending'` is still on the table as a hygiene pass; not blocking.
- **Chrome anchors (forward-only).** Zero existing comments use chrome anchors. The new `chrome-{tag}-{n}` slug shape applies to all future chrome-anchored comments; no orphan risk.
- **Per-tag anchor shape (preserved).** 28 of 29 existing comments resolve cleanly on the new pass: h1/h2/h3/p/span all in canonical; the 3 `<a>` + 1 `<summary>` resolve via `ANCHOR_TAGS_EXTRA`. One `<div>`-anchored comment will orphan to `(no anchor)` (`div` is an anti-pattern in `ANCHOR_TAGS_EXTRA`); the comment is preserved, not deleted.
- **`__chrome__` Firebase RTDB rules.** The deployed rules already accept `__chrome__` as a valid page string (per `work-setup/firebase-rtdb-rules.json`); no rules change needed.

### Static checks performed

39-check static-verification script run on the scratch composition before copy-to-live. All substantive checks passed (38/39); the one "fail" was a comment-text match for `#2c5e4a` in the bootstrap's header documentation explaining the migration *away* from forest green — the actual emitted CSS is correct navy.

Verified:
- 0 bare `.review-*` selectors at column 0 in CSS
- `display: none` baseline on `.review-pill`
- `@media (prefers-reduced-motion: reduce)` guard
- Chrome reset with `all: revert`
- All 5 per-action button hover styles present
- 3 filter tabs in JS
- `getStatus` enum normalizer + `groupStatus` returns filter-tab keys
- `commentLifecycleMode`, `chromeAnchored`, `ANCHOR_TAGS_EXTRA` all read from config
- `CHROME_COUNTERS` separate counter; `chrome-{tag}-{n}` slug shape
- `__chrome__` written on chrome anchors, read in subscription
- `MutationObserver` setup; observer reference stored
- `[data-comment-target]` attribute pass
- `NEVER_ANCHOR` deny-list; `hasDirectText` for direct-text mode
- `isInSiteChrome` uses `'nav, header[role="banner"], footer'`
- New writes use `status: "pending"` enum (not legacy `archived: false`)
- Bootstrap recolored to prota navy; no emoji; `data-review-skip` on entry button

Browser-side verification still required after deploy.

## 2026-05-13 re-emission — corrected library (PR f40cbe7) — SUPERSEDED by 2026-05-15

Re-emitted `review-mode.css` and `review-mode.js` against `library/features/review-widget/` at upstream `f40cbe7` (the corrective PR that aligned the library spec with the deployed reality on prota + glinda). The 2026-05-12 L9 entry below is preserved for history but **superseded** by this entry — the L9 work codified a spec-aspirational pattern that hadn't been verified against deployed reality; the corrective f40cbe7 pulled the deployed reality up into the library and the present re-emission re-derives the deliverables from the corrected spec.

### What the corrected library changed (from what the L9 entry produced)

- **3-state comment lifecycle.** `pending` / `applied` / `archived` enum, where `applied` is a first-class state (build team responded affirmatively) distinct from `archived` (dismissed). Pre-correction prota was 2-state (boolean `archived`); this re-emission adds the third state plus the matching UI + write paths.
- **Class-prefix scope rule clarified.** `.review-` prefix is on chrome ROOTS only; project-local structural classes inside chrome roots (`.text`, `.actions`, `.anchor`, `.group-status`, etc.) do NOT carry the prefix. The deployed prota + glinda pattern already used unprefixed inner-structure classes; the L9 entry's text-on-paper would have prefixed them but the actual L9 emission never did. Net effect on this re-emission: zero change to selector names.
- **Has-comment outline is class-based, not data-attribute-based.** `.has-comment` / `.has-applied-comment` classes on anchored elements (not `[data-has-comments]` attribute). Already aligned in the deployed files; the L9 text-on-paper would have suggested attributes but the L9 emission never used them.
- **Per-action suffix classes for in-row buttons.** `.apply-btn` / `.edit-btn` / `.archive-btn` / `.restore-btn` / `.delete-btn` for the per-row action buttons (modal buttons keep `.review-btn--primary/--secondary/--danger`). Pre-correction prota had three of the five (edit / restore / delete); this re-emission adds the missing two (apply, archive) for the new pending → applied transition.

### Concrete deliverable changes

**`review-mode.js`** (+172 / −45 lines, 32 KB → 38 KB):

- `state.filter` now accepts `"active" | "applied" | "archived"` (was 2-state).
- `isArchived` replaced with `getStatus(c)` reader-side shim returning the enum; thin `isPending` / `isApplied` / `isArchived` wrappers retained for compatibility. Shim normalizes legacy boolean `archived: true|false` and legacy enum values `'applied'` / `'dismissed'` to the canonical enum.
- DEFAULT_LABELS additions: `appliedTab`, `statusApplied`, `applyLabel/applyTitle`, `archiveLabel/archiveTitle`, `applied` / `archived` toast labels, `restoredToPending`. `statusDone` and `restoredToActive` kept as back-compat aliases.
- `decorateAnchors` driven by three-state: any pending → `.has-comment`; all non-archived applied → `.has-applied-comment`; otherwise no class.
- `submitComment` writes `status: "pending"` (was `archived: false`).
- New `applyComment(id)`: writes `status: "applied"` + `applied_at`; dispatches `comment-applied` CustomEvent per `library/events/comment-applied.md` payload shape.
- `archiveComments` writes `status: "archived"` + `archived_at` (was boolean shape).
- `unarchiveComment` writes `status: "pending"` + clears `archived_at` and `applied_at` (was boolean `archived: false`).
- `renderActions(id, status)`: per-status button matrix from `comment-lifecycle.md` §"Per-comment actions in the sidebar". Pending → Apply + Edit + Archive + Delete. Applied → Restore + Archive + Delete. Archived → Restore + Delete.
- Sidebar filter row has three tabs (active / applied / archived); default active.
- `renderCommentList` groups + classifies (active / applied / archived). Filter shows matching groups. Per-row CSS class uses comment status; group container class uses group status. Status badge: `.pending` for active groups, `.applied` for all-applied groups, none for archived (only appear under Archived filter).
- New click handlers for `[data-action='apply']` and `[data-action='archive']`. Restore toast reads `LABELS.restoredToPending` (back-compat alias on label kept).

**`review-mode.css`** (+14 / −4 lines, 24 KB → 25 KB):

- `.apply-btn:hover` added — sage tinted (`var(--review-accent-cool-low)` + sage border).
- `.archive-btn:hover` added — muted tint (`var(--review-surface-tint)` + neutral rule border).
- Per-action hover rules reordered into canonical Apply → Edit → Archive → Restore → Delete order. No other CSS changes; chrome reset, scope prefix, `display: none` baseline, motion guard, `.has-comment` / `.has-applied-comment` outlines, pill container, spotlit class, `.group-status.applied` (already sage-tinted) — all kept verbatim from the L9 emission.

**`work-setup/firebase-rtdb-rules.json`** — comments rule updated:

- `.indexOn`: `[page, anchor, status]` (was `[page, anchor, archived]`).
- CREATE branch requires `status` matching `/^(pending|applied|archived)$/` plus `page` / `anchor` / `timestamp` (was: required `archived: bool`).
- UPDATE branch: page+anchor still immutable; if new data carries a `status` field, must match the enum.
- DELETE branch unchanged.

### One-shot record migration

Per the operator-action note in `comment-lifecycle.md` §"Backward compatibility — reader-side shim", the 9 prota records on RTDB at re-emission time were rewritten from the boolean shape to the enum (`archived: true` → `status: "archived"`; otherwise → `status: "pending"`). Migration performed via REST PATCH against `/comments/{id}.json` after the new rules were published. The reader-side shim stays in place anyway for robustness against any future write paths that haven't migrated.

### Static-isolation checks (per `css-isolation.md` §"Verifying isolation at composition time")

Statically verified against the scratch files (`public/website-composed/`) before copy to live:

1. 0 bare unscoped `.review-*` selectors ✓
2. Chrome reset `all: revert` present (2 occurrences) ✓
3. `display: none` baseline on `.review-pill` + hover override via `:hover:not(:has(...))` ✓
4. `prefers-reduced-motion` guard present (3 occurrences) ✓
5. `position: relative` on `[data-comment-id]` ✓
6. JS parses cleanly via `node --check` ✓
7. 3 filter tabs in `renderSidebar` ✓
8. All 5 per-action button classes referenced in `renderActions` ✓

Browser verification at `protastudios.ai/?review=1` after sync workflow runs.

## 2026-05-12 re-emission — css-isolation contract (L9) — SUPERSEDED by 2026-05-13

Re-emitted `review-mode.css` and class references in `review-mode.js` against the new `library/features/review-widget/css-isolation.md` atomic (upstream commit `1fa1501`). The L9 spec landed after glinda's third-adoption pass surfaced widget-chrome inconsistency across projects; prota's pre-L9 review-mode.css emitted bare `.review-*` selectors (host-page styles could cascade into widget chrome) and used `opacity`-driven pill hide/show (host-page rules could leave the pill visible after cursor exit). This re-emission honors the contract:

- Every CSS rule is now scoped under `html[data-review-mode="on"]` (108 scope-prefix occurrences in the rewritten file; 0 bare `.review-*` selectors). The bootstrap-set attribute on `<html>` (already present in pre-L9 review-mode.js at line 168) gates the entire stylesheet, so it remains inert until the widget is activated.
- Chrome reset block emitted at top of CSS — `all: revert; font-family: var(--review-font-sans); box-sizing: border-box; line-height: 1.4;` on banner / sidebar / sidebar-toggle / modal / modal-backdrop / toast / pill / pill-container. `box-sizing: border-box` propagated to all descendants. Host-page cascade can no longer bleed into widget chrome.
- Pill baseline changed from `opacity: 0; pointer-events: none;` to `display: none;` (the non-negotiable spec-mandated baseline per `pill-hover-deepest.md` §"Required emission"). Hover override uses `display: inline-flex` via the high-specificity `[data-review-mode="on"] [data-comment-id]:hover:not(:has([data-comment-id]:hover)) > .review-pill-container > .review-pill` selector.
- `.review-pill-container` wrapper introduced. The container is positioned absolutely as a host slot with `pointer-events: none` (empty container doesn't intercept clicks on host element); the pill inside takes `pointer-events: auto`. JS: pill DOM construction in `wireAnchors()` now creates the container first, then appends the pill into it.
- Button isolation block — `.review-btn` base + BEM variants `.review-btn--primary / --secondary / --danger` with explicit `font-family: inherit; background: transparent; color: inherit; border; padding; cursor`. Modal cancel/submit buttons rewritten in JS to emit `class="review-btn review-btn--secondary"` and `class="review-btn review-btn--primary"` instead of `class="secondary"` / `class="primary"`.
- Always-on dashed outline on every `[data-comment-id]`: `outline: 1px dashed var(--review-accent-warm-low); outline-offset: 2px; position: relative;` per `anchor-strategy.md` §"Visual treatment". Pre-L9 prota only emitted outlines on hover and on the `.has-comment` state class; spec requires unconditional dashed outline so any anchored element is visually identifiable at rest. Existing `.has-comment` / `.has-applied-comment` state classes preserved.
- Spotlight class renamed `.review-spotlight` → `.review-spotlit` per `css-isolation.md` §"Spotlight class" (CSS + 4 JS `classList` calls in `spotlightAnchor()`).
- `@media (prefers-reduced-motion: reduce)` guard added for spotlight animation (was missing pre-L9).
- SKIP_SELECTOR in JS extended to include `.review-pill-container` alongside `.review-pill` so the container doesn't self-anchor.
- Brand tokens preserved: prota navy primary-deep `#1F3864`, primary-darker `#142646`, amber `#f59e0b`, sage `#4ade80`, slate neutrals. All values now expressed as `--review-*` custom properties at the top of the file rather than literal hex throughout.

No `review-bootstrap.js` change. The bootstrap's `injectKomentariButton` and conditional CSS/JS load remain intact; the widget's `<html data-review-mode="on">` attribute set lives in `review-mode.js` at the existing line 168.

## Sync state

This project's `work-setup/sync-manifest.txt` did not previously include the `library/**` path. As part of this L9 work, the sync-manifest was updated to match upstream (`organization-template/work-setup/sync-manifest.txt`), the `library/` tree was mirrored from upstream, and `.claude/concepts/review-widget.md` was synced. The sync was performed by mirroring from a local upstream clone rather than running `work-setup/sync-conventions.sh --yes`, because the sandbox couldn't authenticate to GitHub. The end-state files are byte-identical to what the script would have pulled.

## Verifying isolation

Verified against `css-isolation.md` §"Verifying isolation at composition time" statically:

1. Inert path: `<html data-review-mode="on">` not set when `?review=1` absent; zero unscoped widget rules (grep confirms 0 bare `.review-*` selectors). ✓
2. Active path: JS sets `<html data-review-mode="on">` at line 168 (unchanged); chrome renders. ✓
3. Hover pill: `display: none` baseline + `:hover:not(:has(...))` override → pill hides on cursor exit. ✓ (static CSS check)
4. Button isolation: `.review-btn` carries `font-family: inherit`, `background: transparent`, `color: inherit`, explicit border + padding; host-page `button { ... }` cannot win against `[data-review-mode="on"] .review-btn` specificity. ✓
5. Has-comment outline: every `[data-comment-id]` gets the always-on dashed outline. ✓

Browser-side verification still required after next deploy — load `?review=1`, hover anchored paragraph, confirm exactly one pill appears, confirm Cancel / Save buttons in the comment modal look like widget variants (navy primary, slate secondary) rather than the host page's site-button style, confirm dashed outline visible on commentable elements.

## Library files referenced

| Layer | Path | Role |
| --- | --- | --- |
| Feature | `library/features/review-widget/FEATURE.md` | Composition spec; entry point |
| Atomic — anchoring | `library/features/review-widget/anchor-strategy.md` | Has-comment outline §"Visual treatment" |
| Atomic — pill hover | `library/features/review-widget/pill-hover-deepest.md` | Display:none baseline + deepest-hover override |
| Atomic — spotlight | `library/features/review-widget/spotlight-on-click.md` | `.review-spotlit` class behavior |
| Atomic — css-isolation | `library/features/review-widget/css-isolation.md` | The L9 contract this re-emission honors |
| Atomic — comment lifecycle | `library/features/review-widget/comment-lifecycle.md` | Modal validation, comment record shape |
| Atomic — RTDB adapter | `library/features/review-widget/firebase-rtdb-adapter.md` | Write path, ordering |
| Atomic — workflow | `library/features/review-widget/pending-archived-workflow.md` | Pending/archived comment states |
| Atomic — intl plural labels | `library/features/review-widget/intl-plural-labels.md` | Plural-aware count labels |
| Component — button | `library/components/button.md` | Button isolation §"Required emission" |

## Variations

This project does not currently have a `.claude/variations/review-widget.md` file. If hand-edits to the composed files become necessary, record them there before editing so a future re-composition preserves the intent.

## Future re-composition

To reproduce or update these deliverables from spec:

1. Run `bash work-setup/sync-conventions.sh --yes` (twice if needed, since the L9 sync added `library/**`).
2. Open this project's review-widget composition session.
3. Read `library/features/review-widget/FEATURE.md` § "What composition produces".
4. Resolve brand tokens (navy / amber / sage values referenced in the CSS custom property block at the top of `review-mode.css`).
5. Emit `review-mode.css`, `review-mode.js`, and (unchanged) `review-bootstrap.js` per the spec's verbatim required emissions.
6. Verify against `css-isolation.md` §"Verifying isolation at composition time" both statically (grep) and in the browser (load `?review=1` on a deploy).
